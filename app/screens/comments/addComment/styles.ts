import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    borderTopColor: '#ccc',
    borderTopWidth: 2,
  },
  input: {
    padding: 10,
    backgroundColor: '#fff',
  },
});
